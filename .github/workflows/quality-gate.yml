name: 🧹 Quality Gate

on:
  push:
    branches: [main, develop] # Only run on push to main/develop (not feature branches)
  pull_request:
    branches: [main] # Run on PRs to main
  workflow_dispatch: # Allow manual triggering for expensive macOS tests
    inputs:
      run_integration_tests:
        description: 'Run expensive Maestro integration tests ($0.08/min)'
        required: false
        default: false
        type: boolean

env:
  EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

jobs:
  # Foundation Layer - Basic Code Quality (run in parallel)
  check-pr-comments:
    name: 💬 Check PR Comments
    runs-on: ubuntu-latest
    timeout-minutes: 2
    if: github.event_name == 'pull_request'

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 🧹 Install slop-mop dependencies
        run: pip install -r slop-mop/requirements.txt --quiet

      - name: 💬 Check for unresolved PR comments
        run: python -m slopmop.sm validate --quality-gates pr:comments
        env:
          PYTHONPATH: slop-mop
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  slop-mop-commit-gates:
    name: 🧹 slop-mop commit gates
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: ⚡ Install Node dependencies
        run: npm ci --legacy-peer-deps

      - name: 🧹 Install slop-mop dependencies
        run: pip install -r slop-mop/requirements.txt --quiet

      - name: 🧹 Run slop-mop commit profile
        run: python -m slopmop.sm validate commit
        env:
          PYTHONPATH: slop-mop

  slop-mop-types-and-deploy:
    name: 🏗️ TypeScript Types + Deploy Tests
    runs-on: ubuntu-latest
    timeout-minutes: 6
    needs: [slop-mop-commit-gates]

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: ⚡ Install Node dependencies
        run: npm ci --legacy-peer-deps

      - name: 🧹 Install slop-mop dependencies
        run: pip install -r slop-mop/requirements.txt --quiet

      - name: 🏗️ TypeScript types + deploy tests
        run: python -m slopmop.sm validate --quality-gates javascript:types,general:deploy-tests
        env:
          PYTHONPATH: slop-mop

  audit-security-vulnerabilities:
    name: 🔒 NPM Security Audit
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: ⚡ Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 🔒 Run NPM Security Audit
        run: npm audit --audit-level high
        continue-on-error: true

  # Integration Layer - Depends on unit tests
  run-maestro-integration-tests:
    name: 🎭 Run Maestro Integration Tests
    runs-on: macos-latest
    timeout-minutes: 60
    needs: slop-mop-commit-gates
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.run_integration_tests == 'true' # Manual trigger only - macOS runners are expensive ($0.08/min)

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: ⚡ Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 🔧 Setup EAS CLI
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 🎭 Execute Maestro Integration Test Suite
        run: ./scripts/run_integration_tests.sh
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: 📸 Upload Test Screenshots and Artifacts
        uses: actions/upload-artifact@v4
        if: always() # Always upload artifacts, even on failure
        with:
          name: maestro-test-artifacts
          path: |
            test_artifacts/
            ~/.maestro/tests/
          retention-days: 365
          if-no-files-found: warn

  verify-expo-build-process:
    name: 🔨 Verify Expo Build Process
    runs-on: ubuntu-latest
    timeout-minutes: 8
    needs: slop-mop-commit-gates
    if: github.event_name == 'pull_request' && github.base_ref == 'main'

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: ⚡ Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 🏗️ Test Expo Export for iOS and Android
        run: |
          echo "🔍 Verifying iOS export..."
          npx expo export --platform ios
          echo "🔍 Verifying Android export..."
          npx expo export --platform android
          echo "✅ Export verification complete"

      - name: 📱 Test EAS Build Configuration
        run: |
          if [ -z "${{ env.EXPO_TOKEN }}" ]; then
            echo "EXPO_TOKEN not available - skipping EAS dry run"
            exit 0
          fi
          npx eas build --platform ios --profile testflight --dry-run
        continue-on-error: true

  # Dependency Analysis - Optional, runs in parallel
  analyze-npm-dependencies:
    name: 📦 Analyze NPM Dependencies
    runs-on: ubuntu-latest
    timeout-minutes: 5
    continue-on-error: true
    if: github.event_name == 'pull_request'

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: ⚡ Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 📦 Check for Outdated Dependencies
        run: |
          echo "🔍 Checking for outdated dependencies..."
          npm outdated || true
          echo "🔍 Checking for unused dependencies..."
          npx depcheck || true
        continue-on-error: true

  # Production Build - Final step, depends on all critical checks passing
  build-for-testflight-production:
    name: 🚀 Build for TestFlight Production
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [slop-mop-commit-gates, slop-mop-types-and-deploy]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: ⚡ Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 🔧 Setup EAS CLI
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 🔍 Verify Production Build Configuration
        run: |
          if [ -z "$EXPO_TOKEN" ]; then
            echo "EXPO_TOKEN not available - skipping EAS build verification"
            exit 0
          fi
          echo "🔍 Verifying TestFlight build configuration..."
          eas build --platform ios --profile testflight --dry-run
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
